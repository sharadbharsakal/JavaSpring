import java.util.*;
public class Main
{
	public static void main(String[] args) {
	
	// Implementing ArrayList - taking integer input store it in ArrayList-
	//-------GDB compiler used--------
	
    ArrayList<Integer> num = new ArrayList();
    
    num.add(76);
    num.add(12);
    num.add(87);
    num.add(54);
    num.add(43);
	
	System.out.println(num);
	
	Iterator <Integer> itr=num.iterator();
    while(itr.hasNext())
    {
    System.out.println(itr.next());
    }
 
	}
}
